%tr
  %td
    = link_to h(page.title), page.url
  -# %td
  -#  = h page.sub_title
  -# %td
  -#   = h page.hint
  %td
    = link_to h(page.url), page.url
  %td
    = h page.page_type
  %td
    = h page.show_timeout
  %td
    = link_to t('pages.share.show'), object_path(page)
    |
    = link_to t('pages.share.edit'), edit_object_path(page)
    |
    = link_to t('pages.share.delete'), object_path(page), :confirm => t('pages.share.delete_confirm', :model => Page.human_name), :method => :delete
